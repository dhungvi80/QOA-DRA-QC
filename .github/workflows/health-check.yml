
name: Health Check

on:
  workflow_dispatch:
  schedule:
    - cron: "55 23 * * *"

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Verify dashboard
        run: |
          test -f dashboard.json
          echo "PASS"
